server:
  port: 8888

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rnzmndz/config.git
#          search-paths: config // use this when you use the link of microservice-component
          default-label: dev
          clone-on-start: true

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://eureka-server:8761/eureka/}

#logging:
#  level:
#    root: DEBUG